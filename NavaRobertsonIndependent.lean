import NavaRobertsonIndependent.Mathematics

/-!
# NRS and NRS³ — Nava–Robertson–Schrödinger Elemental Dimensional Uncertainty

Single verification target of the package: NRS for the pair `(T_d, P_d)` on the path graph and
NRS³ on the product of three path graphs, proved over Mathlib with no physical constant and no
unit. The physical identification (`T_d:P_d` as motion in discrete space, the cube as `x, y, z`)
is a declared bridge, stated in `Mathematics.lean` and in the README, not a theorem.

`Verification/Layer1_Mathematics.lean` checks the import closure and prints
the axioms of the headline theorems.
-/
